---
import Layout from "../layouts/Default.astro";
import Button from "../components/components/Button.astro";
import GoCharbonShortcuts from "../components/GoCharbonShortcuts.astro";
import SentenceQuiz from "../components/vue/SentenceQuiz.vue";
import { getCollection } from "astro:content";
import { filterBuildVisiblePosts } from "../utils/build-posts";
import { filterLaunchParcours, isParcoursOnlyBuild } from "../utils/build-scope";
import { learningPaths } from "../data/parcoursData";
import { launchSignals } from "../data/launchSignals";
import { charbonBadges } from "../gamification/config";
import { ROUTES, getParcoursRoute } from "../config/routes";

const allPosts = filterBuildVisiblePosts(await getCollection("posts"));
const launchBuild = isParcoursOnlyBuild();
const visiblePaths = launchBuild ? filterLaunchParcours(learningPaths) : learningPaths;

const totalPaths = visiblePaths.length;
const totalModules = visiblePaths.reduce((sum, path) => sum + path.modules.length, 0);
const totalSteps = visiblePaths.reduce(
  (sum, path) => sum + path.modules.reduce((moduleSum, module) => moduleSum + module.steps.length, 0),
  0,
);
const averageSteps = Math.round(totalSteps / totalPaths);
const totalBadges = charbonBadges.length;

const popularPathIds = ["freelance", "testeur-utilisateur", "ecommerce", "content-creator", "formation"];
const popularPaths = popularPathIds
  .map((id) => visiblePaths.find((path) => path.id === id))
  .filter((path): path is NonNullable<typeof path> => path != null);

const entryPoints = [
  {
    label: "Vendre un service vite",
    title: "Freelance / conseil / agence",
    description: "Pour partir d’une compétence, trouver des premiers clients et structurer une offre simple.",
    examples: "Exemples : copywriter, assistant virtuel, testeur utilisateur, SEO freelance",
    href: getParcoursRoute("freelance"),
  },
  {
    label: "Revenu annexe cadré",
    title: "Tests utilisateurs / crowdtesting",
    description: "Pour démarrer petit, comprendre vite le terrain et transformer un revenu d’appoint en porte d’entrée vers un service plus structuré.",
    examples: "Exemples : crowdtesting, études rémunérées, missions simples avant une offre freelance plus carrée",
    href: getParcoursRoute("testeur-utilisateur"),
  },
  {
    label: "Construire une audience",
    title: "Contenu / newsletter / activité de créateur",
    description: "Pour avancer avec le contenu, la réputation, la monétisation et un système éditorial tenable.",
    examples: "Exemples : YouTube, newsletter, blog, affiliation",
    href: getParcoursRoute("content-creator"),
  },
  {
    label: "Monter un produit",
    title: launchBuild ? "E-commerce / formation" : "E-commerce / SaaS / formation",
    description: "Pour tester une offre, choisir la bonne structure et poser un vrai chemin de conversion.",
    examples: launchBuild
      ? "Exemples : e-commerce, formation en ligne, offre simple monétisable"
      : "Exemples : e-commerce, SaaS, formation en ligne, live commerce comme angle produit",
    href: getParcoursRoute("ecommerce"),
  },
];

const productPreview = {
  profile: visiblePaths.find((path) => path.id === "ecommerce") ?? popularPaths[0],
  progressPercent: 39,
  xp: 180,
  completedSteps: 5,
  totalSteps: 13,
};
const homeTitle = launchBuild
  ? "GoCharbon - Parcours business pour lancer ton activité"
  : "GoCharbon - Quiz, parcours et progression pour lancer ton business";
const homeDescription = launchBuild
  ? "Cinq parcours pilotes pour lancer une activité crédible, avancer étape par étape et garder des repères utiles."
  : "Trouve un business adapté à ton profil, reçois un parcours concret et avance avec des checklists, de la progression et des outils francophones.";
---

<Layout
  title={homeTitle}
  description={homeDescription}
  pageTitle="GOCHARBON"
  classList="landing-dark-page"
>
  <main class="home-shell min-h-screen">
    <section class="mine-landing" aria-labelledby="mine-landing-title">
      <div class="mine-landing-scene">
        <div class="mine-copy">
          <p class="mine-eyebrow">Lance ton expédition</p>
          <span class="mine-eyebrow-mark" aria-hidden="true"></span>
          <h1 id="mine-landing-title">
            <span class="desktop-hero-title">Creuse moins<br />au hasard.<strong>Trouve le bon filon.</strong></span>
            <span class="mobile-hero-title">Trouve ton filon.<strong>Va au charbon.</strong></span>
          </h1>
          <p class="mine-lead desktop-hero-lead">Un parcours guidé pour transformer une envie de gagner de l’argent en activité solide.</p>
          <p class="mine-lead mobile-hero-lead">Un parcours clair pour transformer tes idées en activité concrète.</p>
          <div class="mine-actions">
            <Button href="#questionnaire-express">
              <span aria-hidden="true">↗</span>
              Trouver mon filon
            </Button>
            <Button href={ROUTES.parcours} variant="secondary">Voir comment ça marche <span aria-hidden="true">›</span></Button>
          </div>
        </div>

        <img
          class="mine-guide"
          src="/images/landing/pixel-miner-guide-v2.png"
          alt="Le mineur GoCharbon, guide de ton exploration"
          width="122"
          height="288"
        />

        <aside class="depth-panel gc-card gc-card--informative" aria-label="Étapes de l’exploration">
          <p>Profondeur</p>
          <ol>
            <li><span>Surface</span><small>Idée brute</small></li>
            <li class="active"><span>Orientation</span><small>Choisir</small></li>
            <li><span>Validation</span><small>Explorer</small></li>
            <li><span>Construction</span><small>Structurer</small></li>
            <li><span>Extraction</span><small>Gagner</small></li>
          </ol>
        </aside>

        <section id="questionnaire-express" class="expedition-panel landing-quiz gc-card gc-card--informative" aria-labelledby="questionnaire-title">
          <div class="pixel-panel-heading">
            <p>Questionnaire express</p>
            <span><i aria-hidden="true"></i> 2 min</span>
          </div>
          <div class="expedition-intro">
            <p>Orientation · Niveau 01</p>
            <h2 id="questionnaire-title">Quel filon mérite vraiment ton énergie&nbsp;?</h2>
            <p>Complète la phrase. On croise ton objectif, ton budget et ton niveau pour te donner une première direction crédible.</p>
          </div>
          <SentenceQuiz client:visible />
          <div class="expedition-proof" aria-label="Garanties du questionnaire">
            <span>Sans inscription</span>
            <span>Résultat immédiat</span>
            <span>Sauvegarde locale</span>
          </div>
        </section>

        <aside class="analysis-stack" aria-label="Aperçu de l’analyse GoCharbon">
          <section class="analysis-panel gc-card gc-card--informative">
            <p class="analysis-title">Console d’analyse</p>
            <div class="analysis-console">
              <p>&gt; objectif : revenu concret</p>
              <p>&gt; budget : à préciser</p>
              <p>&gt; niveau : à préciser</p>
              <p>&gt; traitement des signaux...</p>
              <p class="analysis-positive">&gt; recommandation : prête</p>
              <p>&gt; _</p>
            </div>
          </section>
          <section class="next-step-panel gc-card gc-card--informative">
            <p class="analysis-title">Après le diagnostic</p>
            <strong>Un parcours concret, dans le bon ordre.</strong>
            <Button href={ROUTES.parcours} variant="secondary">Voir les parcours <span aria-hidden="true">›</span></Button>
          </section>
          <section class="reward-panel gc-card gc-card--reward">
            <div>
              <p class="analysis-title">Objectif</p>
              <strong>Passer à l’action</strong>
            </div>
            <img class="pixel-gold" src="/images/icons/pixel-mine-v1/gold-v1.png" alt="" aria-hidden="true" width="56" height="56" />
          </section>
        </aside>
      </div>

      <div class="mine-feature-strip gc-card gc-card--informative" aria-label="Les quatre piliers GoCharbon">
        <a href={ROUTES.parcours}><img src="/images/icons/pixel-mine-v1/map-v1.png" alt="" aria-hidden="true" width="48" height="48" /><div><strong>Parcours guidés</strong><small>Des plans d’action pas à pas.</small></div></a>
        <a href={ROUTES.outils}><img src="/images/icons/pixel-mine-v1/toolbox-v1.png" alt="" aria-hidden="true" width="48" height="48" /><div><strong>Outils de terrain</strong><small>Templates et calculateurs.</small></div></a>
        <a href={ROUTES.blog}><img src="/images/icons/pixel-mine-v1/market-chart-v1.png" alt="" aria-hidden="true" width="48" height="48" /><div><strong>Signaux du marché</strong><small>Des repères pour décider.</small></div></a>
        <a href={ROUTES.progression}><img src="/images/icons/pixel-mine-v1/trophy-v1.png" alt="" aria-hidden="true" width="48" height="48" /><div><strong>Progression</strong><small>Étapes, XP et badges.</small></div></a>
      </div>
    </section>

    <section class="proof-section" aria-labelledby="proof-section-title">
      <div class="px-6 py-14 max-w-6xl mx-auto">
        <div class="section-heading centered">
          <p class="section-kicker">CE QU'IL Y A VRAIMENT DERRIÈRE</p>
          <h2 id="proof-section-title" class="section-title poppins">Un périmètre plus petit, mais des signaux beaucoup plus nets</h2>
          <p class="section-copy">
            Le lancement couvre seulement ce qu’on peut défendre proprement : vendre un service, tester un revenu d’appoint, construire une audience, lancer un produit ou transformer une expertise en formation.
          </p>
        </div>
        <div class="proof-grid">
          <article class="proof-card gc-card gc-card--informative">
            <strong>{totalPaths}</strong>
            <span>parcours reliés à des profils business clairs</span>
          </article>
          <article class="proof-card gc-card gc-card--informative">
            <strong>{totalModules}</strong>
            <span>modules pour passer des bases à l'exécution terrain</span>
          </article>
          <article class="proof-card gc-card gc-card--informative">
            <strong>{totalSteps}</strong>
            <span>étapes actionnables déjà structurées dans le système</span>
          </article>
          <article class="proof-card gc-card gc-card--informative">
            <strong>{launchBuild ? "1 focus" : `${allPosts.length}+`}</strong>
            <span>{launchBuild ? "cinq parcours pilotes assumés avant d'élargir le reste du site" : "guides, tutos et contenus pour te débloquer sans tourner en rond"}</span>
          </article>
        </div>
      </div>
    </section>

    {launchBuild && (
      <section class="signals-section pixel-mine-section">
        <div class="px-6 py-16 max-w-6xl mx-auto">
          <div class="section-heading">
            <p class="section-kicker">POURQUOI CES 5 LÀ D'ABORD</p>
            <h2 class="section-title poppins">On a ouvert d’abord les modèles où les repères sont les plus lisibles</h2>
            <p class="section-copy">
              L’idée n’est pas de tout couvrir dès le jour 1. L’idée est de commencer là où le lecteur peut prendre une décision plus propre, plus vite.
            </p>
          </div>
          <div class="entry-grid">
            {launchSignals.map((signal) => (
              <article class="entry-card gc-card gc-card--informative">
                <p class="entry-label">{signal.kicker}</p>
                <h3>{signal.title}</h3>
                <p>{signal.description}</p>
                <div class="source-links" aria-label={`Sources pour ${signal.title}`}>
                  {signal.sources.map((source) => (
                    <a href={source.href} target="_blank" rel="noopener noreferrer">
                      {source.label}
                    </a>
                  ))}
                </div>
                <p class="entry-cta-row">
                  <Button href={getParcoursRoute(signal.pathId)} variant="secondary">Ouvrir ce parcours →</Button>
                </p>
              </article>
            ))}
          </div>
        </div>
      </section>
    )}

    <section class="difference-section pixel-mine-section pixel-mine-band">
      <div class="px-6 py-16 max-w-6xl mx-auto">
        <div class="section-heading">
          <p class="section-kicker">POURQUOI ÇA TIENT MIEUX DANS LE TEMPS</p>
          <h2 class="section-title poppins">Choisir, exécuter, tenir : dans cet ordre-là</h2>
        </div>
        <div class="difference-grid">
          <article class="difference-card gc-card gc-card--informative">
            <p class="difference-number">01</p>
            <h3>Orientation</h3>
            <p>
              {launchBuild
                ? "Tu commences par des portes d'entrée concrètes. Moins de confusion, moins de dispersion, plus de décision."
                : "Tu commences par un quiz et des portes d'entrée concrètes. Moins de confusion, moins de dispersion, plus de décision."}
            </p>
          </article>
          <article class="difference-card gc-card gc-card--informative">
            <p class="difference-number">02</p>
            <h3>Exécution</h3>
            <p>
              Chaque parcours relie bases, spécialisation et plan 7 jours. Tu vois l'ordre des actions au lieu d'empiler des lectures.
            </p>
          </article>
          <article class="difference-card gc-card gc-card--informative">
            <p class="difference-number">03</p>
            <h3>Motivation visible</h3>
            <p>
              Progression, XP, badges, séries : le site te pousse à terminer quelque chose, pas juste à consommer plus de contenu.
            </p>
          </article>
        </div>
      </div>
    </section>

    <section id="entry-points" class="entry-section pixel-mine-section">
      <div class="px-6 py-16 max-w-6xl mx-auto">
        <div class="section-heading">
          <p class="section-kicker">CHOISIS TA PORTE D'ENTRÉE</p>
          <h2 class="section-title poppins">Commence par le type de machine que tu veux construire</h2>
          <p class="section-copy">
            Au lieu de tomber dans une page catalogue, entre par ton intention : vendre un service, construire une audience, monter un produit ou commencer par un revenu annexe utile.
          </p>
        </div>
        <div class="entry-grid">
          {entryPoints.map((entry) => (
            <article class="entry-card gc-card gc-card--informative">
              <p class="entry-label">{entry.label}</p>
              <h3>{entry.title}</h3>
              <p>{entry.description}</p>
              <p class="entry-examples">{entry.examples}</p>
              <Button href={entry.href} variant="secondary">Ouvrir ce parcours →</Button>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="showcase-section pixel-mine-section pixel-mine-band">
      <div class="px-6 py-16 max-w-6xl mx-auto">
        <div class="section-heading">
          <p class="section-kicker">VOIS LE SYSTÈME AVANT D'ENTRER</p>
          <h2 class="section-title poppins">Tu sais ce que tu vas trouver avant même de cliquer</h2>
        </div>
        <div class="showcase-grid">
          <article class="showcase-card gc-card gc-card--informative">
            <div class="showcase-card-head">
              <span class="showcase-chip">Parcours type</span>
              <strong>3 modules</strong>
            </div>
            <h3>{productPreview.profile.profileTitle}</h3>
            <p>{productPreview.profile.estimatedDuration}</p>
            <ul class="module-list">
              {productPreview.profile.modules.map((module, index) => (
                <li class:list={["module-item", index === 0 ? "done" : null]}>
                  <span class="module-mark">{index === 0 ? "✓" : index === 1 ? "•" : "→"}</span>
                  <div>
                    <strong>{module.title}</strong>
                    <p>{module.objective}</p>
                  </div>
                </li>
              ))}
            </ul>
            <div class="showcase-footer">
              <Button href={getParcoursRoute(productPreview.profile.id)}>
                Voir un vrai parcours
              </Button>
            </div>
          </article>

          <article class="showcase-card dashboard-card gc-card gc-card--informative">
            <div class="showcase-card-head">
              <span class="showcase-chip">Progression</span>
              <strong>Ce que tu gardes sous les yeux</strong>
            </div>
            <div class="dashboard-grid">
              <div class="dashboard-stat gc-card gc-card--informative">
                <strong>180 XP</strong>
                <span>momentum visible</span>
              </div>
              <div class="dashboard-stat gc-card gc-card--informative">
                <strong>3 jours</strong>
                <span>série de lecture</span>
              </div>
              <div class="dashboard-stat gc-card gc-card--informative">
                <strong>2 badges</strong>
                <span>premiers repères</span>
              </div>
              <div class="dashboard-stat gc-card gc-card--informative">
                <strong>39%</strong>
                <span>parcours en cours</span>
              </div>
            </div>
            <div class="badge-row">
              <span>👣 Premier pas</span>
              <span>🔥 Régulier</span>
              <span>💼 Expert business</span>
            </div>
            <p class="dashboard-copy">
              L'idée n'est pas de te distraire avec de la gamification. L'idée, c'est de rendre visible ton avancée pour que tu tiennes assez longtemps pour voir des résultats.
            </p>
            <div class="showcase-footer dual">
              {launchBuild ? (
                <>
                  <Button href={ROUTES.parcours} variant="secondary">
                    Voir tous les parcours
                  </Button>
                  <Button href="#entry-points">
                    Revoir les portes d'entrée
                  </Button>
                </>
              ) : (
                <>
                  <Button href={ROUTES.progression} variant="secondary">
                    Voir la progression
                  </Button>
                  <Button href={ROUTES.quizAvance}>
                    Affiner le quiz
                  </Button>
                </>
              )}
            </div>
          </article>
        </div>
      </div>
    </section>

    {!launchBuild && <GoCharbonShortcuts />}

    <section class="popular-section pixel-mine-section">
      <div class="px-6 py-16 max-w-6xl mx-auto">
        <div class="section-heading">
          <p class="section-kicker">PARCOURS PILOTES</p>
          <h2 class="section-title poppins">Les 5 parcours pilotes ouverts au lancement</h2>
          <p class="section-copy">
            Chacun a été resserré pour qu’un lecteur comprenne vite le modèle, les pièges, les chiffres à suivre et la première action à faire ce soir.
          </p>
        </div>
        <div class="popular-grid">
          {popularPaths.map((path) => (
            <article class="popular-card gc-card gc-card--informative">
              <p class="popular-category">{path.category}</p>
              <h3>{path.profileTitle}</h3>
              <p>{path.outcome}</p>
              <div class="popular-meta">
                <span>{path.modules.length} modules</span>
                <span>{path.estimatedDuration}</span>
              </div>
              <Button href={getParcoursRoute(path.id)} variant="secondary">Voir le parcours →</Button>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="founder-section pixel-mine-section founder-mine-section">
      <div class="px-6 py-16 max-w-6xl mx-auto founder-grid">
        <div class="founder-badge">
          <div class="founder-medallion">D</div>
          <p class="founder-tag">Fondatrice</p>
        </div>
        <div class="founder-copy">
          <p class="section-kicker">POURQUOI CE SITE EXISTE</p>
          <h2 class="section-title poppins">Diane construit le site qu’elle aurait voulu avoir au début</h2>
          <p>
            Je ne manque pas de contenus sur le business en ligne. J’en manque plutôt des bons. GoCharbon part de là : trop de bruit, trop de promesses molles, et pas assez de systèmes qui aident vraiment à choisir, avancer et tenir.
          </p>
          <p>
            Ici, je ne cherche pas à te noyer sous les frameworks. Je cherche à te donner un cap, des actions et des repères, pour que tu puisses enfin passer du scroll à l’exécution.
          </p>
        </div>
      </div>
    </section>

  </main>
</Layout>

<style>
  .home-shell {
    display: flex;
    flex-direction: column;
    gap: 0;
    position: relative;
    overflow: clip;
  }

  .home-shell::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: radial-gradient(circle at 15% 10%, color-mix(in srgb, var(--gc-primitive-color-brand-cream) 35%, transparent) 0 28%, transparent 60%);
    opacity: 0.32;
    z-index: -1;
  }

  .pixel-mine-section {
    background: var(--gc-semantic-color-mine-depth);
    color: var(--gc-semantic-color-text);
  }

  .pixel-mine-section.pixel-mine-band {
    background: var(--gc-semantic-color-surface);
  }

  .founder-mine-section {
    background: var(--gc-semantic-color-surface-strong);
  }

  .hero-grid,
  .founder-grid {
    display: grid;
    gap: var(--home-grid-gap-lg);
  }

  .hero-grid {
    align-items: center;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    max-width: var(--gc-surface-web-hero-content-maximum);
    min-height: min(760px, calc(100vh - 96px));
  }

  .hero-copy,
  .hero-product {
    display: flex;
    flex-direction: column;
    gap: var(--home-surface-gap);
  }

  .hero-kicker,
  .section-kicker {
  margin: 0;
    font-size: var(--home-kicker-font-size);
    font-weight: 800;
    letter-spacing: var(--home-kicker-letter-spacing);
    text-transform: uppercase;
    opacity: 0.72;
  }

  .hero-title,
  .section-title {
    margin: 0;
    line-height: 0.96;
  }

  .hero-title {
    font-family: var(--gc-semantic-type-display-family);
    font-size: clamp(3rem, 6vw, var(--gc-primitive-font-size-display-large));
    max-width: 12ch;
    letter-spacing: -0.035em;
  }

  .hero-title span {
    display: block;
  }

  .hero-intro,
  .section-copy,
  .founder-copy p,
  .panel-copy,
  .dashboard-copy {
    margin: 0;
    font-size: 1.1rem;
    line-height: 1.6;
  }

  .hero-intro {
    max-width: var(--gc-surface-web-hero-copy-maximum);
  }

  .hero-proofline {
    display: flex;
    flex-wrap: wrap;
    gap: var(--gc-primitive-space-2) var(--gc-primitive-space-5);
    padding-block: var(--gc-primitive-space-3);
    border-block: var(--gc-primitive-border-medium) solid currentColor;
    font-family: var(--gc-semantic-type-action-family);
    font-size: var(--gc-primitive-font-size-caption);
  }

  .hero-proofline strong {
    font-size: var(--gc-primitive-font-size-title-small);
  }

  .hero-points {
    display: grid;
    gap: var(--gc-primitive-space-3);
  }

  .hero-point,
  .hero-panel,
  .hero-point {
    display: grid;
    gap: 0.15rem;
    padding: 0.95rem 1rem;
  }

  .hero-point:hover,
  .hero-panel:hover,
  .hero-point strong,
  .proof-card strong,
  .dashboard-stat strong,
  .preview-stats strong,
  .module-item strong,
  .entry-card h3,
  .popular-card h3 {
    font-family: var(--gc-semantic-type-display-family);
  }

  .hero-actions,
  .preview-stats,
  .proof-grid,
  .difference-grid,
  .entry-grid,
  .source-links,
  .popular-grid,
  .dashboard-grid {
    display: grid;
    gap: var(--home-grid-gap);
  }

  .hero-actions {
    grid-template-columns: max-content max-content;
    align-items: center;
  }

  .hero-secondary-link {
    color: currentColor;
    font-weight: var(--gc-primitive-font-weight-bold);
    text-underline-offset: var(--gc-primitive-space-1);
  }

  .hero-note {
    margin: 0;
    font-size: 0.98rem;
    opacity: 0.78;
  }

  .hero-panel {
    padding: var(--home-surface-padding-sm);
  }

  .hero-panel-quiz {
    background: var(--home-hero-panel-quiz-background);
    position: relative;
  }

  .hero-panel-quiz::before {
    content: "NIVEAU 01 · ORIENTATION";
    display: block;
    margin: calc(var(--home-surface-padding-sm) * -1) calc(var(--home-surface-padding-sm) * -1) var(--gc-primitive-space-4);
    padding: var(--gc-primitive-space-2) var(--gc-primitive-space-3);
    background: var(--gc-semantic-color-text);
    color: var(--gc-semantic-color-background);
    font-family: var(--gc-semantic-type-action-family);
    font-size: var(--gc-primitive-font-size-caption);
    font-weight: var(--gc-primitive-font-weight-bold);
    letter-spacing: var(--gc-primitive-font-letter-spacing-wide);
  }

  .mine-rail {
    position: relative;
    height: var(--gc-surface-web-mine-rail-height);
    overflow: hidden;
    border-top: var(--gc-semantic-shape-border-width) solid var(--gc-semantic-color-border);
    background: var(--gc-primitive-color-brand-rock);
  }

  .mine-rail-rocks {
    position: absolute;
    inset: 0;
    opacity: var(--gc-primitive-opacity-muted);
    background-image: linear-gradient(135deg, transparent 45%, var(--gc-primitive-color-brand-charcoal) 45% 55%, transparent 55%);
    background-size: calc(var(--gc-semantic-shape-pixel-unit) * 8) calc(var(--gc-semantic-shape-pixel-unit) * 8);
  }

  .mine-track {
    position: absolute;
    inset: auto 0 var(--gc-primitive-space-3);
    height: var(--gc-primitive-border-strong);
    background: var(--gc-primitive-color-brand-cream);
    box-shadow: 0 var(--gc-primitive-space-3) 0 var(--gc-primitive-color-brand-gold-deep);
  }

  .gold-vein {
    position: absolute;
    right: 8%;
    top: var(--gc-primitive-space-3);
    width: 22%;
    height: var(--gc-primitive-space-2);
    background: var(--gc-primitive-color-brand-yellow);
    transform: skewX(-35deg);
    box-shadow: calc(var(--gc-semantic-shape-pixel-unit) * -4) var(--gc-primitive-space-3) 0 var(--gc-primitive-color-brand-gold-deep);
  }

  .pixel-miner {
    position: absolute;
    z-index: 1;
    left: 12%;
    bottom: var(--gc-primitive-space-5);
    width: var(--gc-primitive-space-8);
    height: var(--gc-primitive-space-10);
    background: var(--gc-primitive-color-brand-yellow);
    border: var(--gc-semantic-shape-pixel-unit) solid var(--gc-primitive-color-brand-black);
    box-shadow: calc(var(--gc-semantic-shape-pixel-unit) * -2) calc(var(--gc-semantic-shape-pixel-unit) * -2) 0 var(--gc-primitive-color-brand-orange);
  }

  .pixel-miner::before {
    content: "";
    position: absolute;
    left: calc(var(--gc-semantic-shape-pixel-unit) * -2);
    top: calc(var(--gc-semantic-shape-pixel-unit) * -3);
    width: calc(100% + var(--gc-semantic-shape-pixel-unit) * 4);
    height: var(--gc-primitive-space-2);
    background: var(--gc-primitive-color-brand-cream);
    border: var(--gc-semantic-shape-pixel-unit) solid var(--gc-primitive-color-brand-black);
  }

  .pixel-miner i {
    position: absolute;
    right: calc(var(--gc-semantic-shape-pixel-unit) * -4);
    top: var(--gc-primitive-space-2);
    width: var(--gc-primitive-space-4);
    height: var(--gc-semantic-shape-pixel-unit);
    background: var(--gc-primitive-color-brand-cream);
    transform: rotate(-45deg);
    transform-origin: left center;
  }

  .hero-panel-preview {
    background: var(--home-hero-panel-preview-background);
  }

  .panel-header {
    display: grid;
    gap: 0.35rem;
  }

  .panel-header h2,
  .hero-panel-preview h3 {
    margin: 0;
    font-size: var(--home-panel-heading-size);
    line-height: var(--gc-primitive-font-line-height-tight);
  }

  .hero-launch-summary {
    margin: 0;
    font-size: 1.08rem;
    line-height: 1.6;
    font-weight: 700;
  }

  .panel-kicker,
  .entry-label,
  .popular-category,
  .founder-tag {
  margin: 0;
    font-size: var(--home-kicker-font-size);
    font-weight: 800;
    letter-spacing: var(--home-kicker-letter-spacing);
    text-transform: uppercase;
  }

  .hero-sentence-quiz {
    margin-top: 0.4rem;
  }

  .preview-topline,
  .showcase-card-head,
  .popular-meta,
  .preview-progress-label {
    display: flex;
    justify-content: space-between;
    gap: var(--gc-primitive-space-4);
    align-items: center;
  }

  .preview-chip,
  .showcase-chip {
    display: inline-flex;
    align-items: center;
    padding: var(--home-chip-padding);
    border: var(--home-chip-border-width) solid currentColor;
    font-size: var(--home-chip-font-size);
    font-weight: 700;
    text-transform: uppercase;
  }

  .preview-chip.muted {
    opacity: 0.72;
  }

  .preview-progress {
    display: grid;
    gap: var(--gc-primitive-space-2);
    margin-top: var(--gc-primitive-space-3);
  }

  .preview-track {
    height: var(--home-preview-track-height);
    border: var(--home-preview-track-border-width) solid var(--gc-semantic-color-border);
    background: var(--home-preview-track-bg);
  }

  .preview-fill {
    height: 100%;
    background: var(--home-preview-fill-bg);
  }

  .preview-stats {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-top: var(--gc-primitive-space-4);
  }

  .preview-stats div,
  .proof-card,
  .dashboard-stat {
    display: grid;
    gap: 0.15rem;
  }

  .proof-grid,
  .difference-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .entry-grid {
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  }

  .popular-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .proof-card,
  .difference-card,
  .entry-card,
  .popular-card,
  .showcase-card {
    padding: 1.2rem;
  }

  .proof-card strong {
    font-size: var(--home-proof-stat-size);
    line-height: 1;
  }

  .difference-number {
  margin: 0 0 0.4rem;
    font-size: var(--home-difference-number-size);
    font-weight: 800;
    color: var(--gc-semantic-color-action-secondary);
  }

  .difference-card h3,
  .entry-card h3,
  .popular-card h3,
  .showcase-card h3 {
  margin: 0 0 0.45rem;
    font-size: var(--gc-primitive-space-6);
  }

  .difference-card p,
  .entry-card p,
  .popular-card p {
    margin: 0;
    line-height: 1.55;
  }

  .entry-card,
  .popular-card {
    display: grid;
    gap: 0.7rem;
  }

  .entry-examples {
    font-size: 0.95rem;
    opacity: 0.82;
  }

  .source-links {
    grid-template-columns: repeat(auto-fit, minmax(110px, max-content));
    gap: 0.55rem;
  }

  .source-links a {
    display: inline-flex;
    justify-content: center;
    align-items: center;
    min-height: var(--home-source-link-min-height);
    padding: var(--home-source-link-padding);
    border: var(--home-chip-border-width) solid var(--gc-semantic-color-border);
    background: var(--gc-semantic-color-surface);
    color: inherit;
    font-size: var(--home-source-link-font-size);
    font-weight: 700;
    text-decoration: none;
  }

  .entry-cta-row {
    margin: 0;
  }

  .entry-card span {
    font-weight: 700;
  }

  .showcase-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--gc-primitive-space-4);
  }

  .module-list {
    list-style: none;
    padding: 0;
    margin: 1rem 0 0;
    display: grid;
    gap: 0.9rem;
  }

  .module-item {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--gc-primitive-space-3);
    align-items: start;
  }

  .module-item p {
    margin: 0.15rem 0 0;
    line-height: var(--gc-primitive-font-line-height-body);
  }

  .module-mark {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.8rem;
    height: 1.8rem;
    border: var(--home-chip-border-width) solid currentColor;
    font-weight: 800;
  }

  .module-item.done .module-mark {
    background: var(--gc-semantic-color-mineral-value);
  }

  .dashboard-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin-top: var(--gc-primitive-space-4);
  }

  .dashboard-stat {
    padding: 0.9rem;
  }

  .badge-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    margin: 1rem 0;
  }

  .badge-row span {
    display: inline-flex;
    padding: 0.35rem 0.7rem;
    border: var(--home-chip-border-width) solid currentColor;
    font-size: 0.92rem;
    font-weight: 700;
  }

  .showcase-footer {
    display: flex;
    margin-top: var(--gc-primitive-space-5);
  }

  .showcase-footer.dual {
    gap: 0.85rem;
    flex-wrap: wrap;
  }

  .section-heading {
    display: grid;
    gap: var(--gc-primitive-space-3);
    margin-bottom: var(--gc-primitive-space-6);
  }

  .section-heading.centered {
    text-align: center;
  }

  .founder-grid {
    grid-template-columns: 160px minmax(0, 1fr);
    align-items: center;
  }

  .founder-badge {
    display: grid;
    justify-items: center;
    gap: var(--gc-primitive-space-3);
  }

  .founder-medallion {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: var(--home-founder-medallion-size);
    height: var(--home-founder-medallion-size);
    border-radius: var(--gc-primitive-radius-pill);
    border: var(--home-founder-medallion-border-width) solid var(--gc-semantic-color-border);
    background: var(--gc-semantic-color-mineral-value);
    font-family: var(--gc-semantic-type-display-family);
    font-size: var(--home-founder-medallion-font-size);
    box-shadow: var(--gc-primitive-space-2) var(--gc-primitive-space-2) 0 var(--gc-semantic-color-shadow);
  }

  .founder-copy {
    display: grid;
    gap: var(--gc-primitive-space-4);
  }

  .difference-card,
  .entry-card,
  .showcase-card,
  .popular-card {
    color: var(--gc-semantic-color-text);
  }

  .source-links a {
    border-color: var(--gc-semantic-color-border);
    background: var(--gc-semantic-color-surface);
  }

  .preview-track {
    border-color: var(--gc-semantic-color-border);
    background: var(--gc-semantic-color-surface);
  }

  .founder-medallion {
    color: var(--gc-semantic-color-text-on-accent);
  }

  @media (max-width: var(--home-mobile-breakpoint)) {
    .hero-grid,
    .showcase-grid,
    .proof-grid,
    .difference-grid,
    .entry-grid,
    .popular-grid,
    .founder-grid {
      grid-template-columns: 1fr;
    }

    .hero-title {
      max-width: none;
      font-size: clamp(2.7rem, 15vw, 4.2rem);
    }

    .hero-grid {
      min-height: auto;
      padding-top: var(--gc-primitive-space-8);
      padding-bottom: var(--gc-primitive-space-8);
    }

    .hero-product {
      margin-top: var(--gc-primitive-space-4);
    }

    .hero-panel-preview {
      display: none;
    }

    .signals-section,
    .difference-section {
      display: none;
    }

    .preview-stats {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: var(--home-small-breakpoint)) {
    .hero-actions {
      grid-template-columns: 1fr;
    }

    .hero-proofline {
      display: grid;
      grid-template-columns: 1fr 1fr;
    }

    .hero-actions :global(.gc-action),
    .showcase-footer :global(.gc-action),
    .preview-topline,
    .showcase-card-head {
      flex-direction: column;
      align-items: flex-start;
    }

    .dashboard-grid {
      grid-template-columns: 1fr;
    }
  }

  /* Landing desktop — scene canonique « mine pixel » */
  :global(body.landing-dark-page) {
    background: var(--gc-primitive-color-brand-ink);
    color: var(--gc-primitive-color-brand-cream);
  }

  :global(body.landing-dark-page header) {
    position: relative;
    border: 0;
    border-bottom: var(--gc-surface-web-landing-panel-border-width) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 12%, transparent);
    background: var(--gc-primitive-color-brand-ink);
    color: var(--gc-primitive-color-brand-cream);
  }

  :global(body.landing-dark-page header .righteous),
  :global(body.landing-dark-page header a) {
    color: var(--gc-primitive-color-brand-cream);
  }

  .mine-landing {
    position: relative;
    min-height: var(--gc-surface-web-landing-desktop-minimum-height);
    overflow: hidden;
    border-bottom: var(--gc-surface-web-landing-panel-border-width) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 14%, transparent);
    background-color: var(--gc-primitive-color-brand-ink);
    background-image: url("/images/landing/pixel-mine-hero-bg-v1.png");
    background-position: center bottom;
    background-repeat: no-repeat;
    background-size: cover;
    color: var(--gc-primitive-color-brand-cream);
  }

  .mine-landing::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background-image: radial-gradient(circle, var(--gc-primitive-color-brand-gold-deep) 0 var(--gc-semantic-shape-pixel-unit), transparent calc(var(--gc-semantic-shape-pixel-unit) + 1px));
    background-size: calc(var(--gc-semantic-shape-pixel-unit) * 28) calc(var(--gc-semantic-shape-pixel-unit) * 23);
    opacity: 0.12;
  }

  .mine-landing-scene {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(370px, 1.22fr) minmax(92px, 0.28fr) minmax(420px, 1.08fr) minmax(250px, 0.7fr);
    gap: var(--gc-surface-web-landing-panel-gap);
    align-items: center;
    width: min(calc(100% - var(--gc-primitive-space-12)), var(--gc-surface-web-landing-desktop-maximum));
    min-height: 650px;
    margin-inline: auto;
    padding-block: var(--gc-primitive-space-8) var(--gc-primitive-space-16);
  }

  .mine-copy {
    align-self: center;
    max-width: 590px;
    padding-left: clamp(var(--gc-primitive-space-4), 2vw, var(--gc-primitive-space-8));
  }

  .mine-eyebrow,
  .analysis-title,
  .pixel-panel-heading,
  .depth-panel > p {
    margin: 0;
    font-family: var(--gc-semantic-type-action-family);
    font-size: var(--gc-primitive-font-size-caption);
    font-weight: var(--gc-primitive-font-weight-bold);
    letter-spacing: var(--gc-primitive-font-letter-spacing-wide);
    text-transform: uppercase;
  }

  .mine-eyebrow {
    color: color-mix(in srgb, var(--gc-primitive-color-brand-cream) 82%, var(--gc-primitive-color-brand-yellow));
  }

  .mine-eyebrow-mark {
    display: block;
    width: var(--gc-primitive-space-12);
    height: var(--gc-semantic-shape-pixel-unit);
    margin-block: var(--gc-primitive-space-4) var(--gc-primitive-space-6);
    background: var(--gc-primitive-color-brand-yellow);
    box-shadow: var(--gc-primitive-space-3) 0 0 var(--gc-primitive-color-brand-gold-deep);
  }

  .mine-copy h1 {
    margin: 0;
    color: var(--gc-primitive-color-brand-cream);
    font-family: var(--gc-semantic-type-display-family);
    font-size: clamp(3.25rem, 4.6vw, 5.35rem);
    font-weight: var(--gc-primitive-font-weight-black);
    letter-spacing: -0.045em;
    line-height: 0.94;
    text-shadow: var(--gc-semantic-shape-pixel-unit) var(--gc-semantic-shape-pixel-unit) 0 var(--gc-primitive-color-brand-charcoal);
  }

  .mine-copy h1 strong {
    display: block;
    margin-top: var(--gc-primitive-space-3);
    color: var(--gc-primitive-color-brand-yellow);
    font-size: 0.75em;
    line-height: 1;
  }

  .mobile-hero-title,
  .mobile-hero-lead {
    display: none;
  }

  .mine-lead {
    max-width: 42ch;
    margin: var(--gc-primitive-space-6) 0 0;
    color: color-mix(in srgb, var(--gc-primitive-color-brand-cream) 72%, transparent);
    font-size: var(--gc-primitive-font-size-body-large);
    line-height: var(--gc-primitive-font-line-height-body);
  }

  .mine-actions {
    display: flex;
    align-items: center;
    gap: var(--gc-primitive-space-6);
    margin-top: var(--gc-primitive-space-8);
  }

  .mine-guide {
    position: absolute;
    z-index: 0;
    left: 27%;
    bottom: 72px;
    width: clamp(122px, 10vw, 180px);
    height: auto;
    image-rendering: pixelated;
    filter: drop-shadow(var(--gc-primitive-space-2) var(--gc-primitive-space-3) 0 color-mix(in srgb, var(--gc-primitive-color-brand-ink) 88%, transparent));
    mix-blend-mode: screen;
  }

  .depth-panel,
  .expedition-panel,
  .analysis-panel,
  .next-step-panel,
  .reward-panel {
  }

  .depth-panel {
    align-self: stretch;
    padding: var(--gc-primitive-space-5) var(--gc-primitive-space-3);
  }

  .depth-panel > p {
    opacity: var(--gc-primitive-opacity-muted);
  }

  .depth-panel ol {
    display: grid;
    gap: 0;
    margin: var(--gc-primitive-space-5) 0 0;
    padding: 0;
    list-style: none;
  }

  .depth-panel li {
    position: relative;
    display: grid;
    gap: var(--gc-primitive-space-half);
    min-height: 72px;
    padding-left: var(--gc-primitive-space-8);
    font-family: var(--gc-semantic-type-action-family);
    font-size: var(--gc-primitive-font-size-caption);
  }

  .depth-panel li::before {
    content: "";
    position: absolute;
    left: var(--gc-primitive-space-2);
    top: var(--gc-primitive-space-1);
    width: var(--gc-primitive-space-3);
    height: var(--gc-primitive-space-3);
    border: var(--gc-semantic-shape-pixel-unit) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 65%, transparent);
    transform: rotate(45deg);
  }

  .depth-panel li:not(:last-child)::after {
    content: "";
    position: absolute;
    left: var(--gc-primitive-space-three-and-half);
    top: var(--gc-primitive-space-4);
    bottom: calc(var(--gc-primitive-space-1) * -1);
    width: var(--gc-semantic-shape-pixel-unit);
    background: color-mix(in srgb, var(--gc-primitive-color-brand-cream) 52%, transparent);
  }

  .depth-panel li.active {
    color: var(--gc-primitive-color-brand-yellow);
  }

  .depth-panel li.active::before {
    border-color: var(--gc-primitive-color-brand-yellow);
    box-shadow: 0 0 0 var(--gc-primitive-space-1) color-mix(in srgb, var(--gc-primitive-color-brand-yellow) 18%, transparent);
  }

  .depth-panel small {
    opacity: var(--gc-primitive-opacity-muted);
  }

  .expedition-panel {
    position: relative;
    z-index: 2;
    align-self: center;
    min-width: 0;
    overflow: visible;
  }

  .pixel-panel-heading {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--gc-primitive-space-4);
    padding: var(--gc-primitive-space-3) var(--gc-primitive-space-4);
    border-bottom: var(--gc-component-pixel-frame-border-width) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 14%, transparent);
  }

  .pixel-panel-heading span {
    display: inline-flex;
    align-items: center;
    gap: var(--gc-primitive-space-2);
    padding: var(--gc-primitive-space-1) var(--gc-primitive-space-2);
    border: var(--gc-semantic-shape-pixel-unit) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 12%, transparent);
    font-size: 0.72rem;
  }

  .pixel-panel-heading i {
    width: var(--gc-primitive-space-2);
    height: var(--gc-primitive-space-2);
    background: var(--gc-primitive-color-brand-yellow);
    box-shadow: var(--gc-primitive-space-half) var(--gc-primitive-space-half) 0 var(--gc-primitive-color-brand-gold-deep);
  }

  .expedition-intro {
    display: grid;
    gap: var(--gc-primitive-space-2);
    padding: var(--gc-primitive-space-5) var(--gc-primitive-space-5) var(--gc-primitive-space-3);
  }

  .expedition-intro p {
    margin: 0;
    color: color-mix(in srgb, var(--gc-primitive-color-brand-cream) 67%, transparent);
    font-size: var(--gc-primitive-font-size-caption);
    line-height: var(--gc-primitive-font-line-height-body);
  }

  .expedition-intro p:first-child {
    color: var(--gc-primitive-color-brand-yellow);
    font-family: var(--gc-semantic-type-action-family);
    letter-spacing: var(--gc-primitive-font-letter-spacing-wide);
    text-transform: uppercase;
  }

  .expedition-intro h2 {
    margin: 0;
    font-family: var(--gc-semantic-type-display-family);
    font-size: clamp(1.75rem, 2vw, 2.4rem);
    line-height: var(--gc-primitive-font-line-height-tight);
  }

  .landing-quiz {
    --sentence-quiz-color: var(--gc-primitive-color-brand-cream);
    --sentence-quiz-trigger-border-color: var(--gc-primitive-color-brand-yellow);
    --sentence-quiz-trigger-hover-border-color: var(--gc-primitive-color-brand-cream);
    --sentence-quiz-trigger-hover-color: var(--gc-primitive-color-brand-yellow);
    --sentence-quiz-trigger-filled-border-color: var(--gc-primitive-color-brand-yellow);
    --sentence-quiz-trigger-filled-color: var(--gc-primitive-color-brand-black);
    --sentence-quiz-trigger-filled-bg: var(--gc-primitive-color-brand-yellow);
    --sentence-quiz-menu-background: var(--gc-primitive-color-brand-rock);
    --sentence-quiz-menu-border: var(--gc-component-pixel-frame-border-width) solid var(--gc-primitive-color-brand-gold-deep);
    --sentence-quiz-menu-shadow: var(--gc-primitive-space-1) var(--gc-primitive-space-1) 0 var(--gc-primitive-color-brand-black);
  }

  .landing-quiz > :global(.sq) {
    padding: var(--gc-primitive-space-3) var(--gc-primitive-space-5) var(--gc-primitive-space-5);
  }

  .expedition-proof {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border-top: var(--gc-component-pixel-frame-border-width) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 12%, transparent);
  }

  .expedition-proof span {
    padding: var(--gc-primitive-space-3) var(--gc-primitive-space-2);
    color: color-mix(in srgb, var(--gc-primitive-color-brand-cream) 62%, transparent);
    font-size: 0.7rem;
    text-align: center;
    text-transform: uppercase;
  }

  .expedition-proof span:not(:last-child) {
    border-right: var(--gc-semantic-shape-pixel-unit) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 12%, transparent);
  }

  .analysis-stack {
    display: grid;
    gap: var(--gc-surface-web-landing-panel-gap);
  }

  .analysis-panel,
  .next-step-panel,
  .reward-panel {
    padding: var(--gc-primitive-space-4);
  }

  .analysis-panel {
    padding: 0;
  }

  .analysis-title {
    margin-bottom: var(--gc-primitive-space-3);
    color: color-mix(in srgb, var(--gc-primitive-color-brand-cream) 58%, transparent);
    font-size: 0.75rem;
  }

  .analysis-panel > .analysis-title {
    margin: 0;
    padding: var(--gc-primitive-space-3) var(--gc-primitive-space-4);
    border-bottom: var(--gc-component-pixel-frame-border-width) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 14%, transparent);
  }

  .analysis-console {
    margin: var(--gc-primitive-space-4);
    padding: var(--gc-primitive-space-3);
    border: var(--gc-semantic-shape-pixel-unit) solid color-mix(in srgb, var(--gc-primitive-color-brand-black) 88%, transparent);
    background: color-mix(in srgb, var(--gc-primitive-color-brand-ink) 76%, transparent);
    font-family: var(--gc-semantic-type-action-family);
    font-size: 0.76rem;
    line-height: 1.55;
  }

  .analysis-console p {
    margin: 0;
  }

  .analysis-positive {
    color: var(--gc-primitive-color-brand-yellow);
  }

  .next-step-panel strong {
    display: block;
    font-size: var(--gc-primitive-font-size-body);
    line-height: 1.35;
  }

  .next-step-panel a {
    display: flex;
    justify-content: space-between;
    margin-top: var(--gc-primitive-space-4);
    padding: var(--gc-primitive-space-2) var(--gc-primitive-space-3);
    border: var(--gc-semantic-shape-pixel-unit) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 24%, transparent);
    color: var(--gc-primitive-color-brand-cream);
    font-size: var(--gc-primitive-font-size-caption);
    text-decoration: none;
  }

  .reward-panel {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .reward-panel strong {
    font-family: var(--gc-semantic-type-action-family);
  }

  .pixel-gold {
    width: var(--gc-primitive-size-avatar-large);
    height: var(--gc-primitive-size-avatar-large);
    object-fit: contain;
    image-rendering: pixelated;
    text-shadow: var(--gc-semantic-shape-pixel-unit) var(--gc-semantic-shape-pixel-unit) 0 var(--gc-primitive-color-brand-gold-deep);
  }

  .mine-feature-strip {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    width: min(calc(100% - var(--gc-primitive-space-12)), var(--gc-surface-web-landing-desktop-maximum));
    margin: calc(var(--gc-primitive-space-12) * -1) auto var(--gc-primitive-space-6);
  }

  .mine-feature-strip a {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--gc-primitive-space-4);
    align-items: center;
    min-width: 0;
    padding: var(--gc-primitive-space-4) var(--gc-primitive-space-5);
    color: var(--gc-primitive-color-brand-cream);
    text-decoration: none;
  }

  .mine-feature-strip a:not(:last-child) {
    border-right: var(--gc-semantic-shape-pixel-unit) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 15%, transparent);
  }

  .mine-feature-strip a > img {
    width: var(--gc-primitive-size-icon-hero);
    height: var(--gc-primitive-size-icon-hero);
    object-fit: contain;
    image-rendering: pixelated;
  }

  .mine-feature-strip strong,
  .mine-feature-strip small {
    display: block;
  }

  .mine-feature-strip strong {
    font-family: var(--gc-semantic-type-action-family);
    font-size: var(--gc-primitive-font-size-caption);
    letter-spacing: var(--gc-primitive-font-letter-spacing-wide);
    text-transform: uppercase;
  }

  .mine-feature-strip small {
    margin-top: var(--gc-primitive-space-1);
    color: color-mix(in srgb, var(--gc-primitive-color-brand-cream) 62%, transparent);
  }

  .proof-section {
    position: relative;
    border-bottom: var(--gc-component-pixel-frame-border-width) solid var(--gc-semantic-color-border);
    background: var(--gc-semantic-color-surface);
    color: var(--gc-semantic-color-text);
  }

  .proof-section::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background-image: linear-gradient(
      to bottom,
      color-mix(in srgb, var(--gc-semantic-color-mineral-value) 10%, transparent),
      transparent var(--gc-primitive-space-16)
    );
  }

  .proof-section > div {
    position: relative;
  }

  .proof-section .section-heading {
    max-width: var(--gc-primitive-size-content-narrow);
    margin-inline: auto;
  }

  .proof-section .section-kicker {
    color: var(--gc-semantic-color-mineral-value);
    font-family: var(--gc-semantic-type-action-family);
    opacity: 1;
  }

  .proof-section .section-title {
    font-family: var(--gc-semantic-type-display-family);
    line-height: var(--gc-primitive-font-line-height-tight);
  }

  .proof-section .section-copy {
    color: var(--gc-semantic-color-text-muted);
  }

  .proof-section .proof-grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .proof-section .proof-card {
    position: relative;
    overflow: hidden;
    color: var(--gc-semantic-color-text);
  }

  .proof-section .proof-card strong {
    color: var(--gc-semantic-color-mineral-value);
    font-family: var(--gc-semantic-type-display-family);
  }

  .proof-section .proof-card span {
    color: var(--gc-semantic-color-text-muted);
    line-height: var(--gc-primitive-font-line-height-body);
  }

  :global([data-theme="dark"]) .proof-section {
    background: var(--gc-semantic-color-mine-depth);
  }

  @media (max-width: var(--home-mobile-breakpoint)) {
    .proof-section .proof-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: var(--home-small-breakpoint)) {
    .proof-section .proof-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 1279px) {
    .mine-landing-scene {
      grid-template-columns: minmax(320px, 0.9fr) minmax(390px, 1.1fr) minmax(220px, 0.6fr);
    }

    .depth-panel {
      display: none;
    }

    .mine-guide {
      left: 26%;
      opacity: 0.82;
    }
  }

  @media (max-width: 900px) {
    .mine-landing-scene {
      grid-template-columns: 1fr;
      width: min(calc(100% - var(--gc-primitive-space-8)), 720px);
    }

    .mine-guide,
    .analysis-stack {
      display: none;
    }

    .mine-copy {
      padding-left: 0;
    }

    .mine-feature-strip {
      grid-template-columns: repeat(2, 1fr);
      margin-top: 0;
    }
  }

  @media (max-width: 640px) {
    .mine-landing {
      min-height: 0;
      background-position: 42% top;
      background-size: auto 720px;
    }

    .mine-landing-scene {
      display: flex;
      flex-direction: column;
      width: 100%;
      min-height: 0;
      gap: var(--gc-primitive-space-5);
      padding: var(--gc-primitive-space-6) var(--gc-primitive-space-3) var(--gc-primitive-space-5);
    }

    .mine-copy {
      width: 100%;
      padding-inline: var(--gc-primitive-space-2);
    }

    .mine-eyebrow,
    .mine-eyebrow-mark,
    .desktop-hero-title,
    .desktop-hero-lead,
    .mine-actions {
      display: none;
    }

    .mobile-hero-title,
    .mobile-hero-lead {
      display: block;
    }

    .mine-copy h1 {
      font-size: clamp(2.8rem, 14vw, 4rem);
      letter-spacing: -0.025em;
      line-height: 0.94;
    }

    .mine-copy h1 strong {
      margin-top: var(--gc-primitive-space-2);
      color: var(--gc-primitive-color-brand-cream);
      font-size: 0.82em;
    }

    .mine-lead {
      max-width: 31ch;
      margin-top: var(--gc-primitive-space-4);
      font-size: 0.95rem;
    }

    .mine-guide {
      position: relative;
      display: block;
      left: auto;
      bottom: auto;
      align-self: center;
      width: 112px;
      margin: var(--gc-primitive-space-2) 0 var(--gc-primitive-space-1);
      mix-blend-mode: normal;
      opacity: 1;
    }

    .expedition-panel {
      order: 3;
      width: 100%;
    }

    .pixel-panel-heading {
      justify-content: center;
      padding: var(--gc-primitive-space-4) var(--gc-primitive-space-3) var(--gc-primitive-space-2);
      border-bottom: 0;
      text-align: center;
    }

    .pixel-panel-heading p {
      font-size: 1.05rem;
    }

    .pixel-panel-heading span,
    .expedition-intro p:first-child,
    .expedition-intro h2,
    .expedition-proof {
      display: none;
    }

    .expedition-intro {
      padding: 0 var(--gc-primitive-space-4) var(--gc-primitive-space-2);
      text-align: center;
    }

    .expedition-intro p:last-child {
      color: color-mix(in srgb, var(--gc-primitive-color-brand-cream) 78%, transparent);
      font-size: 0.78rem;
    }

    .landing-quiz > :global(.sq) {
      padding: var(--gc-primitive-space-3);
    }

    .depth-panel {
      order: 4;
      display: block;
      width: 100%;
      padding: var(--gc-primitive-space-4);
    }

    .depth-panel > p::after {
      content: " 6 m";
      color: var(--gc-primitive-color-brand-yellow);
    }

    .depth-panel ol {
      grid-template-columns: repeat(5, 1fr);
      margin-top: var(--gc-primitive-space-4);
    }

    .depth-panel li {
      min-height: 54px;
      padding: 30px 2px 0;
      font-size: 0.56rem;
      text-align: center;
    }

    .depth-panel li::before {
      left: 50%;
      transform: translateX(-50%) rotate(45deg);
    }

    .depth-panel li:not(:last-child)::after {
      top: 8px;
      left: calc(50% + 10px);
      right: calc(-50% + 10px);
      bottom: auto;
      width: auto;
      height: var(--gc-semantic-shape-pixel-unit);
    }

    .depth-panel small {
      display: none;
    }

    .analysis-stack {
      order: 5;
      display: block;
      width: 100%;
    }

    .analysis-panel {
      display: grid;
      grid-template-columns: auto 1fr;
      align-items: center;
      padding: var(--gc-primitive-space-3) var(--gc-primitive-space-4);
    }

    .analysis-panel > .analysis-title {
      padding: 0;
      border: 0;
      margin: 0;
      color: var(--gc-primitive-color-brand-cream);
    }

    .analysis-console {
      margin: 0;
      padding: 0 0 0 var(--gc-primitive-space-4);
      border: 0;
      background: transparent;
    }

    .analysis-console p { display: none; }
    .analysis-console p:nth-child(4) { display: block; color: var(--gc-primitive-color-brand-yellow); }
    .next-step-panel,
    .reward-panel { display: none; }

    .mine-feature-strip {
      grid-template-columns: repeat(2, 1fr);
      width: calc(100% - var(--gc-primitive-space-6));
      margin: 0 auto var(--gc-primitive-space-5);
    }

    .mine-feature-strip a {
      grid-template-columns: 36px 1fr;
      gap: var(--gc-primitive-space-2);
      min-height: 84px;
      padding: var(--gc-primitive-space-3);
    }

    .mine-feature-strip a:nth-child(2) { border-right: 0; }
    .mine-feature-strip a:nth-child(-n + 2) { border-bottom: var(--gc-semantic-shape-pixel-unit) solid color-mix(in srgb, var(--gc-primitive-color-brand-cream) 15%, transparent); }
    .mine-feature-strip a > img { width: 36px; height: 36px; }
    .mine-feature-strip strong { font-size: 0.68rem; }
    .mine-feature-strip small { display: none; }
  }

</style>
