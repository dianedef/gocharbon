---
import type { CardProps } from "../../utils/types/ui";

type Props = CardProps;

const {
	as: Tag = 'div',
	variant = 'informative',
} = Astro.props;
---

<Tag class:list={["gc-card", `gc-card--${variant}`, "brutal-card-astro"]}>
	<slot />
</Tag>
