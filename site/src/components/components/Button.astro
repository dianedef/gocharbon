---
import type { ButtonProps } from "../../utils/types/ui";
import { designTokens } from "../../generated/design-tokens";

type Props = ButtonProps & { variant?: 'default' | 'hero' };

const {
	href,
	target = "_self",
	color = designTokens.light['semantic.color.actionPrimary'],
	darkColor = designTokens.dark['semantic.color.actionPrimary'],
	variant = 'default'
} = Astro.props;

const shadowColor = variant === 'hero' ? 'var(--brand-yellow)' : 'var(--brand-yellow)';
const rel = target === "_blank" ? "noopener noreferrer" : undefined;
---

<a
	href={href}
	target={target}
	rel={rel}
	class="brutal-btn brutal-btn-astro"
	style={`--button-color: ${color}; --button-shadow-color: ${shadowColor};`}
>
	<slot />
</a>
