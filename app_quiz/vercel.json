{
  "version": 2,
  "installCommand": "echo \"Skipping install (Flutter build)\"",
  "buildCommand": "bash scripts/vercel_build_flutter.sh",
  "outputDirectory": "flutter_app/build/web",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
